# Claude Context

Read these files before Container Compose Adapter work:

- `AGENTS.md`
- `README.md` when it exists
- `docs/plans/index.md` when working on plans or roadmap items

When this repository is checked out as an EMSI monorepo submodule, also read
the parent repository's root `AGENTS.md` before changing the submodule pointer.

Keep this file as a pointer only. The source of truth is the files above.
